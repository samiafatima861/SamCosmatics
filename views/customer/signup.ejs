<!doctype html>
<html>
<head>
  <%- include('../partials/head', { title: 'Create Account', styles: ['products', 'home', 'admin'] }) %>
</head>
<body>

  <%- include('../partials/site-header', { cartCount: typeof cartCount !== 'undefined' ? cartCount : 0 }) %>

  <main class="container page-hero" style="max-width:460px;margin:3rem auto">
    <h1>Create Account</h1>

    <div class="info-card">
      <% if (typeof error !== 'undefined') { %>
        <div class="alert-error"><%= error %></div>
      <% } %>

      <form method="post" action="/signup" class="form-grid">
        <div class="form-field">
          <label>Full name</label>
          <input name="name" required/>
        </div>
        <div class="form-field">
          <label>Email</label>
          <input name="email" type="email" required/>
        </div>
        <div class="form-field">
          <label>Password</label>
          <input name="password" type="password" required/>
        </div>
        <div class="form-field">
          <label>Phone (optional)</label>
          <input name="phone"/>
        </div>
        <div class="form-field">
          <label>Address (optional)</label>
          <textarea name="address"></textarea>
        </div>
        <div>
          <button type="submit" class="btn btn-pink">Create Account</button>
        </div>
      </form>

      <p style="margin-top:1rem">Already have an account? <a href="/login">Log in</a></p>
    </div>
  </main>

  <%- include('../partials/site-footer') %>
</body>
</html>