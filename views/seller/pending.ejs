<!doctype html>
<html>
<head>
  <%- include('../partials/head', { title: 'Account Pending', styles: ['products', 'home', 'order'] }) %>
</head>
<body>

  <%- include('../partials/site-header', { cartCount: typeof cartCount !== 'undefined' ? cartCount : 0 }) %>

  <main class="container">
    <div class="order-confirm">
      <h1>Account Pending Approval</h1>
      <p>Hi <%= seller.name %>, your seller account is still awaiting admin review.</p>
      <p>You'll be able to list products once approved.</p>
      <form action="/seller/logout" method="post" style="margin-top:1.5rem">
        <button type="submit" class="btn btn-dark">Logout</button>
      </form>
    </div>
  </main>

  <%- include('../partials/site-footer') %>
</body>
</html>