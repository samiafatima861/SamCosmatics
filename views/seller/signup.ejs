<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Seller Signup</title>
  <link rel="stylesheet" href="/css/products.css"/>
  <link rel="stylesheet" href="/css/admin.css"/>
</head>
<body>
  <header class="site-header"><div class="container"><a class="logo" href="/">SamCosmetics</a></div></header>
  <main class="container admin-main" style="max-width:500px">
    <h1>Become a Seller</h1>
    <p style="color:#7A6C68;margin-top:-.5rem">Apply to sell your products on SamCosmetics.</p>

    <% if (typeof error !== 'undefined') { %>
      <div class="alert-error"><%= error %></div>
    <% } %>

    <form method="post" action="/seller/signup" class="form-grid">
      <div class="form-field">
        <label>Full name</label>
        <input name="name" required/>
      </div>
      <div class="form-field">
        <label>Email</label>
        <input name="email" type="email" required/>
      </div>
      <div class="form-field">
        <label>Password</label>
        <input name="password" type="password" required/>
      </div>
      <div class="form-field">
        <label>Phone (optional)</label>
        <input name="phone"/>
      </div>
      <div class="form-field">
        <label>Business address (optional)</label>
        <textarea name="address"></textarea>
      </div>
      <div>
        <button type="submit" class="btn btn-pink">Submit Application</button>
      </div>
    </form>

    <p style="margin-top:1rem">Already have an account? <a href="/seller/login">Log in</a></p>
  </main>
</body>
</html>