<!-- <!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Admin Login</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/css/products.css" />
  
  <link rel="stylesheet" href="/css/admin.css" />
</head>
<body>
  <main class="container" style="max-width:480px;margin:4rem auto">
    <h1>Admin Login</h1>
    <% if (typeof error !== 'undefined') { %>
      <div style="color:#b00;margin-bottom:1rem"><%= error %></div>
    <% } %>
    <form method="post" action="/admin/login">
      <div style="margin-bottom:.75rem">
        <label>Email</label><br/>
        <input name="email" type="email" required style="width:100%;padding:.5rem"/>
      </div>
      <div style="margin-bottom:.75rem">
        <label>Password</label><br/>
        <input name="password" type="password" required style="width:100%;padding:.5rem"/>
      </div>
      <div>
        <button type="submit" class="btn" style="background:#d63384;color:#fff;padding:.6rem 1rem;border:none;border-radius:6px">Sign in</button>
      </div>
    </form>
  </main>
</body>
</html> -->




<!-- <!doctype html>
<html>
<head>
   <meta charset="utf-8" />
  <title>Admin Login</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/products.css" />
  <link rel="stylesheet" href="/css/login.css" /> 
   <%- include('../partials/head', { title: 'Admin Login', pageCss: 'login' }) %>
</head>
<body>
  <div class="login-overlay-page">

    <!--blurred live homepage behind the login prompt -->
    <!-- <iframe class="login-bg-frame" src="/" tabindex="-1" aria-hidden="true"></iframe>
    <div class="login-scrim"></div>

    <div class="login-modal-wrap">
      <main class="login-modal">
        <div class="login-eyebrow">Admin Console</div>
        <h1>Admin Login</h1>
        <p class="login-sub">Sign in to manage your store.</p>

        <% if (typeof error !== 'undefined') { %>
          <div class="login-error"><%= error %></div>
        <% } %>

        <form method="post" action="/admin/login">
          <div class="login-field">
            <label>Email</label>
            <input name="email" type="email" required autofocus/>
          </div>
          <div class="login-field">
            <label>Password</label>
            <input name="password" type="password" required/>
          </div>
          <div>
            <button type="submit" class="login-submit">Sign in</button>
          </div>
        </form>
      </main>
    </div>

  </div>
</body>
</html> --> 


<!doctype html>
<html>
<head>
  <%- include('../partials/head', { title: 'Admin Login', pageCss: 'login' }) %>
</head>
<body>
  <div class="login-overlay-page">

    <!-- blurred live homepage behind the login prompt -->
    <iframe class="login-bg-frame" src="/" tabindex="-1" aria-hidden="true"></iframe>
    <div class="login-scrim"></div>

    <div class="login-modal-wrap">
      <main class="login-modal">
        <div class="login-eyebrow">Admin Console</div>
        <h1>Admin Login</h1>
        <p class="login-sub">Sign in to manage your store.</p>

        <% if (typeof error !== 'undefined') { %>
          <div class="login-error"><%= error %></div>
        <% } %>

        <form method="post" action="/admin/login">
          <div class="login-field">
            <label>Email</label>
            <input name="email" type="email" required autofocus/>
          </div>
          <div class="login-field">
            <label>Password</label>
            <input name="password" type="password" required/>
          </div>
          <div>
            <button type="submit" class="login-submit">Sign in</button>
          </div>
        </form>
      </main>
    </div>

  </div>
</body>
</html>
