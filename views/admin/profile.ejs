<!-- <!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Admin Profile</title>
  <link rel="stylesheet" href="/css/products.css" />
  <link rel="stylesheet" href="/css/admin.css" />
</head>
<body>
  <header class="site-header">
    <div class="container" style="display:flex;align-items:center;justify-content:space-between">
      <a class="logo" href="/">SamCosmetics</a>
      <!-- <div>
        <a href="/admin">Dashboard</a>
        <form action="/admin/logout" method="post" style="display:inline"><button class="btn" style="margin-left:.5rem">Logout</button></form>
      </div> -->
      <!-- <div style="display:flex;align-items:center;gap:.75rem">
         <a href="/admin/profile" style="display:flex;align-items:center;gap:.5rem;text-decoration:none;color:inherit">
         <img src="<%= admin?.image_url || '/images/placeholder.png' %>" alt="profile" style="width:32px;height:32px;border-radius:50%;object-fit:cover;border:1px solid #ddd"/>
         <span><%= admin?.name %></span>
         </a>
         <form action="/admin/logout" method="post" style="display:inline"><button class="btn" style="margin-left:.5rem">Logout</button></form>
       </div>
    </div>
  </header>

  <main class="container" style="max-width:800px;margin:2rem auto">
    <h1>Profile</h1>

    <% if (typeof error !== 'undefined') { %>
      <div style="color:#b00"><%= error %></div>
    <% } %>
    <% if (typeof success !== 'undefined') { %>
      <div style="color:green"><%= success %></div>
    <% } %>

    <form action="/admin/profile/edit" method="post" enctype="multipart/form-data" style="display:grid;gap:.75rem">
      <div>
        <label>Name</label><br/>
        <input name="name" value="<%= admin?.name || '' %>" style="width:100%;padding:.5rem"/>
      </div>

      <div>
        <label>Email (readonly)</label><br/>
        <input name="email" value="<%= admin?.email || '' %>" readonly style="width:100%;padding:.5rem;background:#f5f5f5"/>
      </div>

      <div>
        <label>Phone</label><br/>
        <input name="phone" value="<%= admin?.phone || '' %>" style="width:100%;padding:.5rem"/>
      </div>

      <div>
        <label>Address</label><br/>
        <textarea name="address" style="width:100%;padding:.5rem"><%= admin?.address || '' %></textarea>
      </div>

      <div>
        <label>Profile image</label><br/>
        <% if (admin?.image_url) { %>
          <img src="<%= admin.image_url %>" alt="admin" style="width:80px;height:80px;border-radius:8px;display:block;margin-bottom:.5rem"/>
        <% } %>
        <input type="file" name="image" accept="image/*"/>
      </div>

      <div>
        <button type="submit" class="btn" style="background:#d63384;color:#fff;padding:.6rem 1rem;border:none;border-radius:6px">Save profile</button>
      </div>
    </form>

    <hr style="margin:1.5rem 0"/>

    <h2>Change password</h2>
    <form action="/admin/profile/password" method="post" style="display:grid;gap:.5rem;max-width:400px">
      <div><label>Current password</label><br/><input name="current" type="password" required style="width:100%;padding:.5rem"/></div>
      <div><label>New password</label><br/><input name="newPassword" type="password" required style="width:100%;padding:.5rem"/></div>
      <div><button type="submit" class="btn" style="background:#444;color:#fff;padding:.5rem .75rem;border:none;border-radius:6px">Change password</button></div>
    </form>
  </main>
</body>
</html> --> 


<!doctype html>
<html>
<head>
  <%- include('../partials/head', { title: 'Edit Profile', pageCss: 'admin' }) %>
</head>
<body>
  <header class="site-header admin-header">
    <div class="container admin-header-inner">
      <a class="logo" href="/">SamCosmetics</a>
      <div class="admin-nav">
        <a href="/admin/profile" class="admin-profile-link">
          <img src="<%= admin?.image_url || '/images/placeholder.png' %>" alt="profile" class="admin-avatar-sm"/>
          <span><%= admin?.name %></span>
        </a>
        <form action="/admin/logout" method="post" style="display:inline">
          <button class="btn btn-dark btn-logout">Logout</button>
        </form>
      </div>
    </div>
  </header>

  <main class="container admin-main">
    <h1>Profile</h1>

    <% if (typeof error !== 'undefined') { %>
      <div class="alert-error"><%= error %></div>
    <% } %>
    <% if (typeof success !== 'undefined') { %>
      <div class="alert-success"><%= success %></div>
    <% } %>

    <form action="/admin/profile/edit" method="post" enctype="multipart/form-data" class="form-grid">
      <div class="form-field">
        <label>Name</label>
        <input name="name" value="<%= admin?.name || '' %>"/>
      </div>

      <div class="form-field">
        <label>Email (readonly)</label>
        <input name="email" value="<%= admin?.email || '' %>" readonly/>
      </div>

      <div class="form-field">
        <label>Phone</label>
        <input name="phone" value="<%= admin?.phone || '' %>"/>
      </div>

      <div class="form-field">
        <label>Address</label>
        <textarea name="address"><%= admin?.address || '' %></textarea>
      </div>

      <div class="form-field">
        <label>Profile image</label>
        <% if (admin?.image_url) { %>
          <img src="<%= admin.image_url %>" alt="admin" class="admin-avatar-square"/>
        <% } %>
        <input type="file" name="image" accept="image/*"/>
      </div>

      <div>
        <button type="submit" class="btn btn-pink">Save profile</button>
      </div>
    </form>

    <hr style="margin:1.5rem 0"/>

    <h2>Change password</h2>
    <form action="/admin/profile/password" method="post" class="form-grid form-grid--narrow">
      <div class="form-field">
        <label>Current password</label>
        <input name="current" type="password" required/>
      </div>
      <div class="form-field">
        <label>New password</label>
        <input name="newPassword" type="password" required/>
      </div>
      <div>
        <button type="submit" class="btn btn-dark">Change password</button>
      </div>
    </form>
  </main>
</body>
</html>
